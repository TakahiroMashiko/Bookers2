<!-- 投稿の更新が失敗したとき、フラッシュメッセージを表示 -->
<!-- １個のエラーだったら、「1 error prohibited this obj from being saved:」を、２個のエラーだったら「2 errors prohibited this obj from being saved:」を表示（else文で行けそう） -->
<% if @book.errors.any? %>
  <%= @book.errors.count %> errors prohibited this obj from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<h1>Editing Book</h1>
<!-- form_with部分 -->
<%= form_with model: @book, local:true do |f| %>
  <label for="inputTitle">Title</label>
  <%= f.text_field :title, autofocus: true, id:"inputTitle", placeholder:"Title" %>
  <label for="inputOpinion">Opinion</label>
  <%= f.text_area :body, autofocus: true, id:"inputOpinion", placeholder:"Body" %>

  <%= f.submit 'Update Book' %>
<% end %>

<%= link_to "Show", book_path(@book.id) %> | <%= link_to "Back", books_path %>