<!-- ユーザのプロフィール更新が失敗したとき、フラッシュメッセージを表示 -->
<!-- １個のエラーだったら、「1 error prohibited this obj from being saved:」を、２個のエラーだったら「2 errors prohibited this obj from being saved:」を表示（else文で行けそう） -->
<p>
<% if @user.errors.any? %>
  <%= @user.errors.count %> errors prohibited this obj from being saved:
  <% @user.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>
</p>
<!-- userの編集画面（User info）-->
<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2>User info</h2>
      <!-- form_with部分 -->
      <%= form_with model: @user, local:true do |f| %>
        <div class="form-group">
          <label for="user_name">Name</label>
          <%= f.text_field :name, autofocus: true, id:"user_name", class: "form-control name" %>
        </div>
        <div class="form-group">
          <label for="user_Image">Image</label>
          <%= f.attachment_field :profile_image, id:"user_profile_image", class: "form-control-file user_image" %>
        </div>
        <div class="form-group">
          <label for="user_introduction">Introduction</label>
          <%= f.text_area :introduction, autofocus: true, id:"user_introduction", class: "form-control introduction" %>
        </div>
        <div class="form-group">
          <%= f.submit 'Update User', class: "btn btn-secondary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>