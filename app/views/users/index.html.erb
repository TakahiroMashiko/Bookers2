<h1>Users</h1>
<!--< % @users.each do |user| %>-->
  <p>Name</p>
  <span>
    <!-- < %=link_to user.name, user_path(user.id) %>-->
  </span>
  <p>Image</p>
  <%= attachment_image_tag @user, :image, :fill, 100, 100, format: 'jpeg' %>

<!-- < % end %>-->